syntax = "proto3";

package hostApi;

service Host {
    rpc createRoom(Room) returns (Room);
    rpc deleteRoom()
}

message voidMsg {}

message roomReq {
    string UUID = 1;
}

message room {
    int32 id = 1;
    string str = 2;
}
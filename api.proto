syntax = "proto3";

package hostApi;

service Host {
    rpc createRoom(roomReq) returns (room);
    rpc deleteRoom(roomReq) returns (voidMsg);
}

message voidMsg {}

message roomReq {
    string UUID = 1;
}

message room {
    string UUID = 1;
}